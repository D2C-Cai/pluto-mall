server:
  port: 8081

spring:
  profiles:
    active: dev

  application:
    name: oauth2-service

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml

management:
  endpoints:
    web:
      exposure:
        include: "*"
